import React from "react";
import { makeStyles } from "@material-ui/core/styles";
import Card from "@material-ui/core/Card";
import CardContent from "@material-ui/core/CardContent";
import Button from "@material-ui/core/Button";
import Typography from "@material-ui/core/Typography";
import CardHeader from "@material-ui/core/CardHeader";
import CardMedia from "@material-ui/core/CardMedia";
import FormControl from "@material-ui/core/FormControl";
import TextField from "@material-ui/core/TextField";
import Divider from "@material-ui/core/Divider";
import { updateCharacterPoints } from "../../helpers/selectors";
import { render } from "react-dom";
import MakeNewCharacterList from "./MakeNewCharacterList"
import MakeNewCharacterCard from "./MakeNewCharacterCard"
import CharacterCard from "../CharacterCard";
import CharacterList from "../CharacterList";

export default function NewCharacter() {
  
  return (


<CharacterList />
// components 
    //drop downlist of characters pulled from heros table
    //character card shows currently selected character  
    //Form entry for name
    //make character button (post to database)
    //list of current characters (component character list)

    
  )
 
}